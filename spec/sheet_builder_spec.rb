require 'spec_helper'

describe SheetBuilder do
  it 'has a version number' do
    expect(SheetBuilder::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
